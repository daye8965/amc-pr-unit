# amc-pr-unit

서울아산병원 **언론유닛 홍보 실적 대시보드 (2021–2026)**.
연도별 보도자료·언론보도 실적을 탭으로 나눠 보고, PDF로 출력해 보고서로 씁니다.

- 공개 주소: https://daye8965.github.io/amc-pr-unit/

## 구조

단일 파일 정적 페이지입니다. `index.html` 하나가 전부이고 (`test.html`은 실험용), 빌드 과정이 없습니다.

- 라이브러리: Chart.js, html2canvas, jsPDF (CDN)
- 탭: `overview`(전체 현황) / `compare`(연도별 비교) / `y2026`(2026년 상세) / `dept`(진료과 분석) / `staff`(담당자 현황) / `workload`(업무량 분석) / `terms`(키워드·브랜딩)
- 탭을 추가하면 `상단 nav 버튼` / `tabOrder` / `tabNames` / `<section id="sec-*">` / `show()` 분기 다섯 군데를 같은 순서로 맞춰야 합니다(순서가 어긋나면 탭 활성화와 PDF 제목이 엇갈립니다).

## 데이터 (중요)

**데이터는 저장소에 없습니다.** 페이지가 열릴 때 Google Apps Script 엔드포인트(`GOOGLE_APPS_SCRIPT_URL`, `index.html` 상단)를
`fetch`해서 Google Sheets의 실적을 받아 `ALL_DATA`에 채웁니다.

받아오는 필드: `year, month, type, staff, title, dept, media, doctor`

따라서:

- **실적을 고치려면 저장소가 아니라 Google Sheet를 고쳐야 합니다.** 코드를 바꿔도 수치는 변하지 않습니다.
- 시트나 Apps Script 배포가 막히면 대시보드가 빈 화면이 됩니다. 수치가 안 나올 때 코드부터 의심하지 마세요.
- 오프라인에서는 동작하지 않습니다.

## 작업할 때 주의

- 모든 차트는 하드코딩 값이 아니라 `ALL_DATA`에서 계산하도록 정리해 둔 상태입니다. 새 차트도 `ALL_DATA` 기준으로 만드세요.
- 보도자료는 **연도+월+제목** 키로 중복 제거합니다(`type === '보도자료'`인 행만). 이 키가 깨지지 않게 하세요.
- 시트 **헤더행**(`year`가 `년도`)이 API 응답에 섞여 옵니다. `year`가 네 자리 숫자인 행만 남기는 필터가 로드 단계에 있습니다. 지우지 마세요.
- `staff` 칸에는 공동 담당이 **줄바꿈으로 여러 명** 들어있고(66건), `.문지애` 같은 오타와 `유닛공통`·`다같이` 같은 팀 표기도 있습니다.
  사람 단위로 집계할 때는 `wlStaffNames()`를 쓰세요(분리·정규화 담당). 문자열 그대로 세면 공동 담당이 유령 담당자가 됩니다.
- `type`은 시트에 20종 넘게 있습니다. `TYPES_ORDER`는 6종만 다루므로, 새 분석은 상위 N종 + `그 외` 방식을 권장합니다.
- Apps Script 엔드포인트가 간헐적으로 404(HTML 오류 페이지)를 돌려줍니다. `fetchSheetWithRetry()`가 3회 재시도하니 이 감싸기를 걷어내지 마세요.
- 진료과·담당자·연도 필터가 서로 엮여 있으니, 한쪽을 고치면 나머지 필터에서도 확인하세요.
- PDF 출력이 주 용도입니다. 레이아웃을 바꾸면 PDF 저장 결과까지 확인하세요.
- PDF는 **탭 하나 = A4 가로 한 페이지**로 통째 캡처합니다. 세로로 긴 탭은 비율을 유지한 채 작게 들어가므로, 섹션이 길어지면 가로 2단 배치를 먼저 검토하세요.
- 커밋 메시지는 영문 명령형 한 줄이 많습니다.
- 확인: `python -m http.server 8765` 후 `http://localhost:8765/` (시트 접근이 되어야 데이터가 뜹니다)

## 규칙 관리

- 새 규칙·주의사항이 생기면 사용자가 세션 중에 "이거 CLAUDE.md에 추가해줘"라고 말합니다.
  그때는 이 파일의 알맞은 섹션에 한 줄로 덧붙이세요(새 섹션 남발 금지).
- 즉, 이 파일이 유일한 규칙 저장소입니다. 규칙을 다른 곳에 따로 메모하지 마세요.
